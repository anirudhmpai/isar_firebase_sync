import 'package:flutter/material.dart';

class BaseController extends ChangeNotifier {
  var isLoading = false;

  void startLoading() {
    isLoading = true;
    notifyListeners();
  }

  void stopLoading() {
    isLoading = false;
    notifyListeners();
  }
}
